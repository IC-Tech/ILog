{
  "version": 2,
  "name": "ILog",
  "builds": [
    { "src": "package.json", "use": "@now/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/$1" },
    { "src": ".*", "dest": "/index.html" }
  ]
}
